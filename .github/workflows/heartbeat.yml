name: Supabase Heartbeat

on:
  schedule:
    # ทุก 5 วัน เวลา 03:00 UTC (keep-alive Supabase free tier)
    - cron: '0 3 */5 * *'
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Ping heartbeat Edge Function
        run: |
          curl -fsS -X GET \
            "https://nnsxyuhiwgrqbszrhyiz.supabase.co/functions/v1/heartbeat" \
            -H "Authorization: Bearer sb_publishable_qMqdfCN5EcUiBh1zNcBVBQ_G-qmeQx5" \
            -H "apikey: sb_publishable_qMqdfCN5EcUiBh1zNcBVBQ_G-qmeQx5"
